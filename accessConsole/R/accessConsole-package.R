#' accessConsole: Admin Console for Managing Apps, Roles, Permissions, and Groups
#'
#' The admin console Shiny app for a shared, multi-app identity system.
#' Authentication and SSO are delegated to \pkg{authClient}; this package
#' owns the admin CRUD and UI. See `run_admin_console()` to start it.
#'
#' @keywords internal
#' @import shiny
"_PACKAGE"
