% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth-sessions.R
\name{create_session}
\alias{create_session}
\title{Create a new SSO session}
\usage{
create_session(
  con,
  user_id,
  session_secret = Sys.getenv("AUTH_SESSION_SECRET"),
  idle_timeout_secs = 8 * 3600,
  created_by_app = NA_character_,
  user_agent = NA_character_,
  ip_address = NA_character_
)
}
\arguments{
\item{con}{A DBI connection or pool object.}

\item{user_id}{The user's ID.}

\item{session_secret}{Shared HMAC key; must be identical across every
app that should share SSO sessions. Defaults to the
`AUTH_SESSION_SECRET` environment variable.}

\item{idle_timeout_secs}{Sliding idle timeout, in seconds.}

\item{created_by_app}{The calling app's `app_key`, recorded for
audit/debug purposes. `NA` if not applicable (e.g. the console's own
login, which has no single app to attribute it to).}

\item{user_agent}{Optional user-agent string to record.}

\item{ip_address}{Optional IP address to record.}
}
\value{
A list with `cookie_value`, `session_id`, `expires_at`, and
  `max_age_secs`.
}
\description{
Generates a fresh session secret, stores a keyed hash of it in the
`sessions` table, and returns the cookie value to hand to the browser.
Cookie value is `"<session_id>.<raw_secret_hex>"`. Only `secret_hash` (a
keyed HMAC of the raw secret) is ever stored -- the raw secret exists
only in the browser's cookie and in-memory during login/rotation.
}
