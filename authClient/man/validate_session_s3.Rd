% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth-sessions-s3.R
\name{validate_session_s3}
\alias{validate_session_s3}
\title{Validate an SSO session cookie (S3-backed)}
\usage{
validate_session_s3(
  con,
  cookie_value,
  session_secret = Sys.getenv("AUTH_SESSION_SECRET"),
  idle_timeout_secs = 8 * 3600,
  absolute_ttl_secs = 30 * 24 * 3600,
  rotate_after_secs = 30 * 60
)
}
\arguments{
\item{con}{A DBI connection or pool object -- used only to fetch a
fresh username/status for the session's user (see the note at the
top of this file for why that isn't denormalized into S3 instead).}
}
\value{
A list with `ok`, and on success `user_id`, `username`,
  `new_cookie_value` (non-`NULL` only if rotated), and `max_age_secs`.
}
\description{
Validate an SSO session cookie (S3-backed)
}
